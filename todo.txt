+save session id at server and check if user exists with this session id
+load items of category (in category.jsp)
+warenkorb C
+admin benutzersystem
-search for item (optional) B
+login ausblenden wenn eingeloggt, loggout ausblenden wenn ausgeloggt
+alle feedbacks mit angular
+fehlerbehandlung (feedback)
-input validieren (mit ng-show (cart.jsp) oder ng-valid) B
-compile source files with maven (für abgabe) B
+category already exists in db should return "creation not possible" C
+db path relativ -> also tests must run C
-test register, login, logout (weil jetzt alles über angular)
	+REST test C
	-Client test B
+server side tests ob session_id und username existieren C
-paypal B
+angular umstellung für login, register C
+add to cart button at categories (when items listed) C
+logout -> destroy cookie C

info: user privilegien == 7 admin, < 7 alle anderen, 0 nicht eingeloggt
	admin erstellen per hand! bal ist bereits admin
